# Use SvelteKit for the Web Application

Daily uses SvelteKit as the full-stack web framework. SvelteKit supports the required authenticated web UI, server-side endpoints, form-driven settings, email preview routes, and shared TypeScript code while keeping the application in a single project.
