# Use Resend for Email Delivery

Daily uses Resend to send scheduled and test Daily Summary emails. Resend gives the application a focused transactional email API for HTML summaries, while Delivery Records keep only delivery metadata rather than storing rendered email content.
