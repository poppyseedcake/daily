# Use Vitest and Playwright for Testing

Daily uses Vitest for unit and integration tests and Playwright for key end-to-end UI workflows. Tests should cover time-zone scheduling, summary eligibility, email rendering, Todo reordering, worker idempotency, and the Visitor-to-User Local Setup flow so the most failure-prone behavior is protected from the start.
