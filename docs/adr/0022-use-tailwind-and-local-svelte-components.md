# Use Tailwind and Local Svelte Components

Daily uses Tailwind CSS with local Svelte components rather than a heavy UI kit. This supports a restrained, minimalist application interface tailored to Daily's dashboard, Todo, settings, preview, and Admin Panel workflows, with `lucide-svelte` used for icons.
