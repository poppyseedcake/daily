# Use Drizzle ORM for Database Access

Daily uses Drizzle ORM for typed database access and migrations. Drizzle fits the TypeScript and SQLite stack, keeps queries close to SQL, and avoids adding a heavier runtime layer for the application's straightforward relational data model.
