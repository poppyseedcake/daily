# Use Better Auth for Authentication

Daily uses Better Auth for Google sign-in, sessions, and Google account linking. Better Auth has first-class SvelteKit integration, supports Google as a social provider, and can use the existing Drizzle and SQLite persistence stack through its Drizzle adapter, keeping authentication in the same TypeScript application rather than a separate auth service.
