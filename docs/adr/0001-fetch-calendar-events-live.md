# Fetch Calendar Events Live

Daily fetches Calendar Events from Google Calendar when generating a Daily Summary instead of storing copied calendar event data. This keeps the summary aligned with the latest calendar state and reduces the amount of private calendar data retained by the application, at the cost of depending on Google Calendar availability during summary generation.
