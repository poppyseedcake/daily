# Use Open-Meteo for Weather

Daily uses Open-Meteo for Weather Section forecasts. Open-Meteo supports the simple daily forecast data the product needs and keeps the first version lightweight, with Weather Locations stored as user-chosen cities and resolved coordinates rather than retained forecast snapshots.
