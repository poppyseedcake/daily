# Use GitHub Actions for CI

Daily uses GitHub Actions from the start to run dependency installation, Svelte checks, tests, and production builds on pushes and pull requests. This keeps the TypeScript, SvelteKit, Drizzle, scheduling, and email-rendering workflow protected before deployment to the VPS.
