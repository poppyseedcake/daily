# Auto-Save Local Setup After Google Sign-In

Daily lets Visitors explore and configure the application before signing in, then automatically saves that Local Setup to the User after Google sign-in. This removes friction from first use and preserves work the Visitor already did, accepting that sign-in is also the moment when local browser data becomes account data without an extra confirmation step.
